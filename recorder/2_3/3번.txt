1번
1.1 증감 활용
1.2 배열의 인덱스 활용


2번
퀵 정렬 : 분할과 재귀
기준값(피봇)을 중심으로 자료를 왼쪽 부분집합과 오른쪽 부분집합으로 분할
왼쪽 부분집합으로 기준값보다 작은 원소 이동, 오른쪽 부분집합으로 기준값보다 큰 원소 이동
- 동일한 값에 대해 기존의 순서가 유지되지 않는 불안정 정렬
- 평균 시간 복잡도 N^2

병합 정렬  : 
여러 개의 정렬된 집합을 병합하여 하나의 정렬된 집합으로 만드는 정렬 방법
자료를 부분집합으로 분할하고 부분집합에 대해 작업을 정복하고 부분집합들을 다시 결합하는 분할과 정복 방법
- 동일한 값에 대해 기존의 순서가 유지되는 안정 정렬
- 시간 복잡도 NlogN


3번
cin : 공백과 개행, 탭 등을 기준으로 문자를 나눔
-공백을 포함한 string을 입력 시, 공백 전까지 string에 입력을 받음
cin.getline() 사용하여 공백을 포함한 string 입력을 받을 수 있음
-구분자는 공백을 포함할 수 있음
cin.get() 사용 사용하여 구분자가 공백을 포함하여 입력을 받을 수 있음


4번
Const after function
- 객체 내부 변수 변경 불가
- const 함수만 호출 가능